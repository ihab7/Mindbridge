# MindBridge — Environment Variables
#
# Copy this file to .env.local for local development:
#   cp .env.example .env.local
# On Vercel, set these under Project Settings → Environment Variables.
#
# This list reflects the variables actually read by the codebase
# (checked via `grep -r process.env`). Do not add unused variables —
# MindBridge does not use NextAuth or JWT today.

# ---------------------------------------------------------------------------
# Database (required)
# ---------------------------------------------------------------------------
# PostgreSQL connection string, read by lib/db.ts via @neondatabase/serverless.
# Must be a Neon (or Neon-compatible HTTP-driver) Postgres connection string.
# Get this from your Neon project dashboard → Connection Details.
# Include ?sslmode=require (Neon requires SSL).
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# ---------------------------------------------------------------------------
# Anthropic (optional — AI-assisted care planning)
# ---------------------------------------------------------------------------
# Read server-side only, by app/api/program/propose/route.ts, to draft an
# AI-assisted anxiety program for a practitioner to review and approve.
# Without it, the feature degrades cleanly to the deterministic rules engine
# (lib/program/ai/rulesFallback.ts) — practitioners can still draft and assign
# a program, it just won't be AI-personalized.
ANTHROPIC_API_KEY=sk-ant-...

# Note: the "Find a Psychiatrist Near You" directory map (OpenStreetMap via
# Leaflet, components/psychiatrist/practitioner-map.tsx) and its address
# geocoder (lib/geocode.ts, via Nominatim) need no API key.

# ---------------------------------------------------------------------------
# Automatically provided — do not set manually
# ---------------------------------------------------------------------------
# NODE_ENV is set automatically by Next.js/Vercel (development, production).
# lib/auth.ts reads it to decide whether the session cookie is marked `secure`.
